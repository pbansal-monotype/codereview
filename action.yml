name: 'AI PR Reviewer'
description: 'AI-powered PR review with configurable company guidelines for security, testing, performance, cost, and custom checks'
author: 'Your Company'

branding:
  icon: 'eye'
  color: 'blue'

inputs:
  provider:
    description: 'AI provider to use: "anthropic", "openai", or "azure"'
    required: true
    default: 'anthropic'

  api_key:
    description: 'API key for the AI provider. If not set, reads from ANTHROPIC_API_KEY, OPENAI_API_KEY, or AZURE_API_KEY secret automatically based on provider.'
    required: false
    default: ''

  azure_endpoint:
    description: 'Azure OpenAI endpoint URL (required when provider is "azure"). Accepts the full deployment URL (e.g. https://<resource>.cognitiveservices.azure.com/openai/deployments/<deployment>/chat/completions?api-version=...) or just the bare resource URL. Can also be set via the AZURE_ENDPOINT env var.'
    required: false
    default: ''

  model:
    description: 'Model to use. For Azure, this is the deployment name (e.g. "gpt-5.4-nano"). For Anthropic: "claude-sonnet-4-20250514". For OpenAI: "gpt-4o".'
    required: false
    default: ''

  github_token:
    description: 'GitHub token for accessing PR data and posting comments. Defaults to the built-in GITHUB_TOKEN.'
    required: false
    default: ${{ github.token }}

  review_categories:
    description: 'Comma-separated list of categories to review: security,tests,performance,code,custom'
    required: false
    default: 'security,tests,performance,code'

  security_guidelines:
    description: 'Custom security review guidelines'
    required: false
    default: ''

  test_guidelines:
    description: 'Custom test case review guidelines'
    required: false
    default: ''

  performance_guidelines:
    description: 'Custom performance review guidelines'
    required: false
    default: ''

  code_guidelines:
    description: 'Custom code quality review guidelines (error handling, correctness, patterns)'
    required: false
    default: ''

  custom_prompt:
    description: 'Repo-specific context sent with the review (tech stack, architecture, conventions)'
    required: false
    default: ''

  extra_instructions:
    description: 'Company-wide policies appended to the system prompt (tone, standards, compliance rules)'
    required: false
    default: ''

  max_diff_size:
    description: 'Maximum diff size in characters before smart truncation (default 60000)'
    required: false
    default: '60000'

  post_review_comment:
    description: 'Post the review summary as a PR comment (true/false)'
    required: false
    default: 'true'

  post_inline_comments:
    description: 'Post inline review comments on specific diff lines (true/false)'
    required: false
    default: 'true'

  fail_on_critical:
    description: 'Fail the action if critical issues are found (true/false)'
    required: false
    default: 'false'

  ignore_paths:
    description: 'Comma-separated glob patterns for files to skip (e.g. "**/migrations/**,**/*.generated.ts")'
    required: false
    default: ''

  redact_secrets:
    description: 'Redact potential secrets from diff before sending to AI (true/false)'
    required: false
    default: 'true'

  timeout:
    description: 'Timeout in seconds for each AI API call (default 120)'
    required: false
    default: '120'

  include_file_contents:
    description: 'Send full contents of changed files alongside the diff for better context (true/false)'
    required: false
    default: 'true'

  context_files:
    description: 'Comma-separated file paths to always include for context (e.g. "src/types.ts,src/db/schema.prisma")'
    required: false
    default: ''

  max_file_size:
    description: 'Maximum characters per file when including file contents (default 10000)'
    required: false
    default: '10000'

outputs:
  review_body:
    description: 'The full review text generated by the AI'
  has_critical_issues:
    description: 'Whether critical issues were found (true/false)'
  categories_reviewed:
    description: 'Comma-separated list of categories that were reviewed'
  findings_count:
    description: 'Number of structured findings returned by the AI'

runs:
  using: 'node20'
  main: 'dist/index.js'
